<UserControl x:Class="rslm_frontend.Views.BuilderTerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource VsBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0" Background="{DynamicResource VsPanelBrush}" Padding="8,4">
            <Label Content="IP Serveur:" Foreground="{DynamicResource VsTextBrush}" Margin="4,0"/>
            <TextBox x:Name="ServerIpBox" Text="127.0.0.1" Width="120" Margin="0,0,8,0"/>
            <Label Content="Port:" Foreground="{DynamicResource VsTextBrush}" Margin="4,0"/>
            <TextBox x:Name="ServerPortBox" Text="4782" Width="60" Margin="0,0,8,0"/>
            <Separator Style="{DynamicResource VsToolBarSeparatorStyle}"/>
            <Button x:Name="GenerateAgentBtn" Click="GenerateAgent_Click" Content="🚀 Générer Agent" Margin="4" Padding="8,4"/>
            <Button x:Name="GenerateStubBtn" Click="GenerateStub_Click" Content="📦 Générer Stub" Margin="4" Padding="8,4"/>
            <Button x:Name="ClearTerminalBtn" Click="ClearTerminal_Click" Content="🗑 Clear" Margin="4" Padding="8,4"/>
        </ToolBar>

        <Border Grid.Row="1" BorderBrush="{DynamicResource VsBorderBrush}" BorderThickness="1" Background="{DynamicResource VsBackgroundBrush}">
            <ScrollViewer x:Name="ScrollViewer" VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="TerminalBox" 
                         IsReadOnly="True"
                         Background="{DynamicResource VsBackgroundBrush}"
                         Foreground="{DynamicResource VsTextBrush}"
                         FontFamily="Consolas"
                         FontSize="11"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Visible"/>
            </ScrollViewer>
        </Border>

        <StatusBar Grid.Row="2" Background="{DynamicResource VsPanelBrush}">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Foreground="{DynamicResource VsSecondaryTextBrush}" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>